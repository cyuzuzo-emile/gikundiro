# TODO

- [ ] Fix login failures (axios baseURL / auth flow) by updating `client/src/services/api.js` to correctly target backend `/api` in both dev and prod.
- [ ] Re-run login and confirm it succeeds (token stored + `/auth/me` works).
- [ ] If still failing, add better UI error reporting and/or inspect backend logs for credential mismatch.

